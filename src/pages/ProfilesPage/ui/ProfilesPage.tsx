import { Page } from '@shared/ui/Page'

const ProfilesPage = () => {
  return (
    <Page
      breadcrumbsList={[{ id: '0', name: 'Profiles', icon: 'account_circle' }]}
      id="profiles-page"
    >
      ProfilesPage
    </Page>
  )
}

export default ProfilesPage
